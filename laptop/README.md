This directory houses code that runs on an external computer that controls the
robot. An external computer is required for the robot to be aware of its own
location, and to process images submitted by the user.
